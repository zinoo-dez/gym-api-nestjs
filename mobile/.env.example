# Use your machine's local IP (e.g., http://192.168.1.5:3000/api) if testing on a real device.
# Prefer 127.0.0.1 over localhost to avoid ::1/IPv6 routing issues.
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000/api
